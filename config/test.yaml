name: reg_0.00001

defaults:
  - base
  - _self_

test:
  device: cuda
  batch_size: 1024
  rank: 20
  embeddings: [
    [baseline with dot, dot, baseline],
    [best with dot, dot, emb_512_n_layers_4_reg_0.001_gcl_temp_0.2_geo_distance_neighbors_4_weight_1.0],
    [layer weighting, dot, emb_512_n_layers_4_reg_0.001_gentle_0.1_gcl]
  ]
  baselines: []
