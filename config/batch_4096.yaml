name: "batch_4096"

defaults:
  - base
  - _self_

training:
  batch_size: 4096
  device: "cuda"
  n_epochs: 1000
  early_stopping: 20
  lr: 0.003
  reg_weight: 0.0001

evaluation:
  batch_size: 4096
  rank: 20

model:
  embedding_dim: 64
  n_layers: 3
